#!/usr/bin/env python
# -*- coding: utf-8 -*-
# generated by wxGlade 0.6.3 on Sun Oct 17 08:58:20 2010

import wx
import urllib
import pprint
from urlparse import parse_qs
# begin wxGlade: extracode
# end wxGlade



class MyFrame1(wx.Frame):
    def __init__(self, *args, **kwds):
        # begin wxGlade: MyFrame1.__init__
        kwds["style"] = wx.DEFAULT_FRAME_STYLE
        wx.Frame.__init__(self, *args, **kwds)
        self.text_ctrl_1 = wx.TextCtrl(self, -1, "", style=wx.TE_MULTILINE|wx.TE_LINEWRAP)
        self.format_radio = wx.RadioBox(self, -1, "Format", choices=["Unicode", "Ascii"], majorDimension=2, style=wx.RA_SPECIFY_COLS)
        self.text_ctrl_2 = wx.TextCtrl(self, -1, "", style=wx.TE_MULTILINE|wx.TE_LINEWRAP)

        self.__set_properties()
        self.__do_layout()

        self.Bind(wx.EVT_TEXT, self.onTxt1, self.text_ctrl_1)
        # end wxGlade

    def __set_properties(self):
        # begin wxGlade: MyFrame1.__set_properties
        self.SetTitle("TamperConvert")
        self.SetSize((623, 477))
        self.text_ctrl_1.SetMinSize((500, 200))
        self.format_radio.SetSelection(0)
        self.text_ctrl_2.SetMinSize((500, 200))
        # end wxGlade

    def __do_layout(self):
        # begin wxGlade: MyFrame1.__do_layout
        sizer_9 = wx.BoxSizer(wx.VERTICAL)
        sizer_9.Add(self.text_ctrl_1, 0, wx.EXPAND|wx.ADJUST_MINSIZE, 0)
        sizer_9.Add(self.format_radio, 0, wx.ADJUST_MINSIZE, 0)
        sizer_9.Add(self.text_ctrl_2, 0, wx.EXPAND|wx.ADJUST_MINSIZE, 0)
        self.SetSizer(sizer_9)
        self.Layout()
        self.SetSize((623, 477))
        # end wxGlade

    def onTxt1(self, event): # wxGlade: MyFrame1.<event_handler>
        orig = self.text_ctrl_1.GetValue()
        new = orig.replace('POSTDATA=','')
        try:
            
            new = urllib.unquote(new)
            new = parse_qs(new)
            if self.format_radio.GetSelection() == 1:
                new = convert(new)
            pp = pprint.PrettyPrinter(indent=4)
            formatted = pp.pformat(new)
            #print new.toString()

        except Exception, myerror:
            print myerror
            formated = new
        
        self.text_ctrl_2.SetValue(formatted)=
        event.Skip()

# end of class MyFrame1

def convert(data):
    if isinstance(data, unicode):
        return str(data)
    elif isinstance(data, dict):
        return dict(map(convert, data.iteritems()))
    elif isinstance(data, (list, tuple, set, frozenset)):
        return type(data)(map(convert, data))
    else:
        return data
if __name__ == "__main__":
    app = wx.PySimpleApp(0)
    wx.InitAllImageHandlers()
    frame_1 = MyFrame1(None, -1, "")
    app.SetTopWindow(frame_1)
    frame_1.Show()
    app.MainLoop()
